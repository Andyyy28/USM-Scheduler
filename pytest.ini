[pytest]
DJANGO_SETTINGS_MODULE = usm_scheduler.settings
python_files = tests.py test_*.py *_tests.py
addopts = -ra --strict-markers -m "not diagnostic"
testpaths = tests
markers =
    e2e: real-browser end-to-end workflow tests
    diagnostic: opt-in performance or research-protocol exercise excluded from the deterministic suite
